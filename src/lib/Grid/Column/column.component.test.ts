import React from 'react'; // eslint-disable-line

describe('Column', () => {
    test('renders Column component', () => {
        // const { container } = render(<App products={products} incidents={incidents} />);
        // const app = container.querySelector('.app');
        const Column = true;

        expect(Column).toBeTruthy();
    });
});
